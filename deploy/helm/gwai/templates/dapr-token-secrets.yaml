{{- if not .Values.security.daprAPI.existingSecret }}
{{- $secretName := include "gwai.daprAPISecretName" . }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $token := randAlphaNum 48 | b64enc }}
{{- if and $existing (hasKey $existing.data "token") }}
{{- $token = index $existing.data "token" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "gwai.labels" . | nindent 4 }}
type: Opaque
data:
  token: {{ $token | quote }}
{{- end }}
{{- if and (not .Values.security.daprAPI.existingSecret) (not .Values.security.appAPI.existingSecret) }}
---
{{- end }}
{{- if not .Values.security.appAPI.existingSecret }}
{{- $secretName := include "gwai.appAPISecretName" . }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $token := randAlphaNum 48 | b64enc }}
{{- if and $existing (hasKey $existing.data "token") }}
{{- $token = index $existing.data "token" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "gwai.labels" . | nindent 4 }}
type: Opaque
data:
  token: {{ $token | quote }}
{{- end }}
{{- if and (not .Values.security.virtualKeyAppAPI.existingSecret) (or (not .Values.security.daprAPI.existingSecret) (not .Values.security.appAPI.existingSecret)) }}
---
{{- end }}
{{- if not .Values.security.virtualKeyAppAPI.existingSecret }}
{{- $secretName := include "gwai.virtualKeyAppAPISecretName" . }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $token := randAlphaNum 48 | b64enc }}
{{- if and $existing (hasKey $existing.data "token") }}
{{- $token = index $existing.data "token" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "gwai.labels" . | nindent 4 }}
type: Opaque
data:
  token: {{ $token | quote }}
{{- end }}
